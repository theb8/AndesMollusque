# Write to Access file

Write to Access file

## Usage

``` r
write_capture_mollusque(df, access_db_write_connection = NULL)
```

## Arguments

- df:

  Dataframe

- access_db_write_connection:

  the connection to Access file
