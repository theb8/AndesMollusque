# Creates a new MS Access database file

Creates a new MS Access database file

## Usage

``` r
create_new_access_db(fname = "new_access_db.mdb")
```

## Arguments

- fname:

  Filename for new access DB
