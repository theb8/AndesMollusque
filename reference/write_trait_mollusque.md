# Write dataframe to Access file

Write dataframe to Access file

## Usage

``` r
write_trait_mollusque(trait, access_db_write_connection = NULL)
```

## Arguments

- trait:

  Dataframe

- access_db_write_connection:

  the connection to Access file
