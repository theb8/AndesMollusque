# Write dataframe to Access file

Write dataframe to Access file

## Usage

``` r
write_freq_long_mollusque(df, access_db_write_connection = NULL)
```

## Arguments

- df:

  Dataframe

- access_db_write_connection:

  connection to Access file
